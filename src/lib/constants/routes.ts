export const appRoutes = {
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
  uploadsNew: "/uploads/new",
  projects: "/projects",
  reviewQueue: "/projects",
  publish: "/publish",
  channels: "/settings/channels"
} as const;
