export const appRoutes = {
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
  createProject: "/projects/new",
  projects: "/projects",
  reviewQueue: "/projects",
  publish: "/publish",
  channels: "/settings/channels"
} as const;
